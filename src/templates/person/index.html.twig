
					<h4> <small><a href="/dashboard/student/add" >Create New</a></small></h4>                    
				         <table id="missing-persons" class="table table-striped table-td-valign-middle">
								<thead>
									<tr>
										<th class="text-nowrap">Full name</th>
										<th class="text-nowrap">Email</th>
										<th class="text-nowrap">Phone</th>
										<th class="text-nowrap">Action</th>
									</tr>
								</thead>
								<tbody>
										{% for item in list %} 
										<tr  class="" >
											<td><a href="/dashboard/student/add/{{item.id}}" >{{item.fullname|length > 15 ? item.fullname|slice(0, 15) ~ '...' : item.fullname}}</a></td>
											<td>{{item.email}}</td>
                                            <td>{{item.phone}}</td>										
											<td>											
											<a href="/dashboard/student/delete/{{item.id}}" >Delete <span class="fa fa-trash text-success"></span></a></td>
										</tr>
										{% endfor %}
										</tbody>
				  			</table>                        							
						</div>
                  </div>
      </section>
	